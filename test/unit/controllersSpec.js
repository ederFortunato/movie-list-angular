'use strict';

/* jasmine specs for controllers go here */
/*
describe('controllers', function(){
  var $scope = null;
  var ctrl = null;
  var mockService = {
    someAsyncCall: function (x){
      return 'weee';
    }
  }

  beforeEach(module('myApp.controllers'));

  it('should munuActive be ""', inject(function($rootScope, $controller) {
    $scope = $rootScope.$new();
    ctrl = $controller('MenuCtrl', {
      $scope: $scope
    });

    expect($scope.munuActive).toEqual('');
  }));

  it('should MyMoviesCtrl....', inject(function($rootScope, $controller) {
    $scope = $rootScope.$new();
    ctrl = $controller('MyMoviesCtrl', {
      $scope: $scope,
      myMoviesService: mockService,
      imdbService: mockService
    });

    expect($scope.getActiveRating(1, 1)).toEqual('selectedStar');


  }));
});
*/